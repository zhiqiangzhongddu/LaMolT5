import openai
import time
import asyncio
import os
from tqdm import tqdm
from prompts_single_rewrite_check_backup import prompts_files_check

# Check that all the rewritings exist and the files are not empty
prompts_files_check()